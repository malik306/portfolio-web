{
  "name": "portfolio-web",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "homepage": "http://malik306.github.io/portfolio-web",
  "scripts": {
    "start": "parcel ./src/pages/index.html --out-dir public -p 3030 watch --no-cache --no-source-maps",
    "build": "parcel build ./src/pages/index.html --out-dir public --public-url ./ --no-cache --no-source-maps",
    "pre": "yarn build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "sass": "^1.38.0"
  }
}
